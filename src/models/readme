La carpeta models sirve para guardar los modelos de datos de nuestra base de datos.

MongoDB puede almacenar cualquier dato pero con el modelo de datos podemos decirle que tipo de dato queremos almacenar y si no cumple con los datos que hemos especificado, se creará un error, es decir, estaremos creando esquemas.